/* Used to override 'react-scripts' settings.
(react-app-rewired provides the ability to hook into the config) 
*/

// eslint-disable-next-line no-unused-vars
function override(config, env) {
  //do stuff with the webpack config...
  return config;
}